(function(root){
'use strict';
const MINIMUM={questions:3375,enem:2220,pism:1155,discursives:52};
const QUESTION_COLUMNS='id,exam,module,area,subject,topic,difficulty,text,options,answer,explanation,skill,source,editorial_status,variant_family,variant_type';
const DISCURSIVE_COLUMNS='id,module,subject,topic,prompt,rubric,editorial_status';
const PAGE_SIZE=1000,DB_NAME='gabarito_plus_content_v2',DB_STORE='bank',DB_KEY='published';

function timeout(promise,ms){let timer;return Promise.race([promise,new Promise((_,reject)=>{timer=setTimeout(()=>reject(new Error('Tempo limite do banco de questões excedido.')),ms)})]).finally(()=>clearTimeout(timer))}
function question(row){return{id:String(row.id),exam:row.exam,module:row.module??null,area:row.area,subject:row.subject,topic:row.topic,difficulty:row.difficulty,text:row.text,options:row.options,answer:Number(row.answer),explanation:row.explanation,skill:row.skill,source:row.source||'Banco Gabarito+',editorialStatus:row.editorial_status||'publicada',variantFamily:row.variant_family||String(row.id),variantType:row.variant_type||'Original'}}
function discursive(row){return{id:String(row.id),module:row.module,subject:row.subject,topic:row.topic,prompt:row.prompt,rubric:Array.isArray(row.rubric)?row.rubric:[]}}
function validate(questions,discursives){const enem=questions.filter(q=>q.exam==='ENEM').length,pism=questions.filter(q=>q.exam==='PISM').length;if(questions.length<MINIMUM.questions||enem<MINIMUM.enem||pism<MINIMUM.pism||discursives.length<MINIMUM.discursives)throw new Error(`Banco de questões incompleto (${questions.length} objetivas, ${discursives.length} discursivas).`);if(new Set(questions.map(q=>q.id)).size!==questions.length)throw new Error('Banco de questões contém IDs duplicados.');if(questions.some(q=>!Array.isArray(q.options)||q.options.length!==5||!Number.isInteger(q.answer)||q.answer<0||q.answer>4))throw new Error('Banco de questões contém questão inválida.');return{questions:questions.length,enem,pism,discursives:discursives.length}}
function normalize(rawQuestions,rawDiscursives,metaRows){const questions=rawQuestions.map(question),discursives=rawDiscursives.map(discursive),counts=validate(questions,discursives),values=Object.fromEntries((metaRows||[]).map(x=>[x.key,x.value]));return{questions,discursives,counts,version:values.question_bank_version||values.app_version||null}}

async function clientPage(client,table,columns,from,to){const {data,error}=await client.from(table).select(columns).eq('editorial_status','publicada').order('id',{ascending:true}).range(from,to);if(error)throw error;return Array.isArray(data)?data:[]}
async function clientAll(client,table,columns,pageSize=PAGE_SIZE){const rows=[];for(let from=0;;from+=pageSize){const batch=await clientPage(client,table,columns,from,from+pageSize-1);rows.push(...batch);if(batch.length<pageSize)break}return rows}
async function load(client,{timeoutMs=9000}={}){if(!client)throw new Error('Conexão com o banco ausente.');return timeout((async()=>{const [rawQuestions,rawDiscursives,meta]=await Promise.all([clientAll(client,'questions',QUESTION_COLUMNS),clientAll(client,'pism_discursives',DISCURSIVE_COLUMNS),client.from('app_meta').select('key,value').in('key',['question_bank_version','app_version'])]);if(meta.error)throw meta.error;return normalize(rawQuestions,rawDiscursives,meta.data||[])})(),timeoutMs)}

function restHeaders(key){return{apikey:key,authorization:`Bearer ${key}`,accept:'application/json'}}
async function restGet(base,key,path,params,signal){const u=new URL(`${base.replace(/\/$/,'')}/rest/v1/${path}`);for(const [k,v] of Object.entries(params||{}))u.searchParams.set(k,String(v));const r=await fetch(u,{headers:restHeaders(key),signal,cache:'no-store'});if(!r.ok)throw new Error(`Supabase ${path}: HTTP ${r.status}`);const data=await r.json();return Array.isArray(data)?data:[]}
async function restAll(base,key,table,columns,signal,pageSize=PAGE_SIZE){const rows=[];for(let offset=0;;offset+=pageSize){const batch=await restGet(base,key,table,{select:columns,editorial_status:'eq.publicada',order:'id.asc',limit:pageSize,offset},signal);rows.push(...batch);if(batch.length<pageSize)break}return rows}
async function loadDirect(config,{timeoutMs=8000}={}){const base=String(config?.url||'').trim(),key=String(config?.publishableKey||'').trim();if(!base||!key)throw new Error('Supabase não configurado.');const controller=new AbortController(),timer=setTimeout(()=>controller.abort(new Error('Tempo limite do banco online excedido.')),timeoutMs);try{const [rawQuestions,rawDiscursives,metaRows]=await Promise.all([restAll(base,key,'questions',QUESTION_COLUMNS,controller.signal),restAll(base,key,'pism_discursives',DISCURSIVE_COLUMNS,controller.signal),restGet(base,key,'app_meta',{select:'key,value',key:'in.(question_bank_version,app_version)'},controller.signal)]);return normalize(rawQuestions,rawDiscursives,metaRows)}catch(e){if(e?.name==='AbortError')throw new Error('Tempo limite do banco online excedido.');throw e}finally{clearTimeout(timer)}}

function dbOpen(){return new Promise((resolve,reject)=>{if(!('indexedDB' in root))return reject(new Error('IndexedDB indisponível.'));const req=indexedDB.open(DB_NAME,1);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(DB_STORE))db.createObjectStore(DB_STORE)};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error||new Error('Falha ao abrir cache.'))})}
async function cacheRead(){try{const db=await dbOpen();return await new Promise((resolve,reject)=>{const tx=db.transaction(DB_STORE,'readonly'),req=tx.objectStore(DB_STORE).get(DB_KEY);req.onsuccess=()=>resolve(req.result||null);req.onerror=()=>reject(req.error);tx.oncomplete=()=>db.close()})}catch{return null}}
async function cacheWrite(bundle){try{const db=await dbOpen();await new Promise((resolve,reject)=>{const tx=db.transaction(DB_STORE,'readwrite');tx.objectStore(DB_STORE).put({...bundle,savedAt:new Date().toISOString()},DB_KEY);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error)});db.close();return true}catch{return false}}
function cachedValid(bundle){try{if(!bundle||!Array.isArray(bundle.questions)||!Array.isArray(bundle.discursives))return false;validate(bundle.questions,bundle.discursives);return true}catch{return false}}
async function remoteVersion(base,key,timeoutMs=1800){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{const rows=await restGet(base,key,'app_meta',{select:'key,value',key:'in.(question_bank_version,app_version)'},controller.signal),values=Object.fromEntries(rows.map(x=>[x.key,x.value]));return values.question_bank_version||values.app_version||null}finally{clearTimeout(timer)}}
async function loadDirectCached(config,{timeoutMs=8000}={}){const base=String(config?.url||'').trim(),key=String(config?.publishableKey||'').trim();if(!base||!key)throw new Error('Supabase não configurado.');const cached=await cacheRead();if(!cachedValid(cached)){const fresh=await loadDirect(config,{timeoutMs});await cacheWrite(fresh);return{...fresh,fromCache:false,stale:false}}
 try{const version=await remoteVersion(base,key,Math.min(2200,timeoutMs));if(version&&cached.version===version)return{...cached,fromCache:true,stale:false};const fresh=await loadDirect(config,{timeoutMs});await cacheWrite(fresh);return{...fresh,fromCache:false,stale:false}}catch(e){return{...cached,fromCache:true,stale:true,cacheError:e?.message||String(e)}}}
async function clearCache(){try{const db=await dbOpen();await new Promise((resolve,reject)=>{const tx=db.transaction(DB_STORE,'readwrite');tx.objectStore(DB_STORE).delete(DB_KEY);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)});db.close();return true}catch{return false}}

root.GabaritoQuestionSource={load,loadDirect,loadDirectCached,clearCache,validate,EXPECTED:MINIMUM,MINIMUM};
})(typeof window!=='undefined'?window:globalThis);
