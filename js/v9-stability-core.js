/* Gabarito+ V9.2 — estabilidade consolidada, sem dependência de IA */
(function(){
'use strict';
const VERSION='9.2.1';
const K={settings:'study_v2_settings',sessions:'study_sessions',stats:'study_question_stats_v2',favorites:'study_favorites_v2',essays:'study_essays_v2',draft:'study_essay_draft_v2',review:'study_review_queue_v25',answers:'study_answer_log_v25',essayState:'gplus_v92_essay_state',mockResume:'gplus_v92_mock_resume'};
const safe=(fn,fallback)=>{try{return fn()}catch(e){console.warn('[Gabarito+ V9.2]',e);return fallback}};
const load=(key,fallback)=>{try{const raw=localStorage.getItem(key);return raw?JSON.parse(raw):fallback}catch{return fallback}};
const save=(key,value)=>{try{localStorage.setItem(key,JSON.stringify(value));return true}catch{return false}};
const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const num=(value,fallback=0)=>Number.isFinite(Number(value))?Number(value):fallback;
const setText=(el,value)=>{value=String(value??'');if(el&&el.textContent!==value)el.textContent=value};
const setHtml=(el,value)=>{if(el&&el.innerHTML!==value)el.innerHTML=value};
const today=()=>{const d=new Date();return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const questions=()=>safe(()=>window.QuestionBank?.getAll?.()||[],[]);
const settings=()=>({...{dailyGoal:60,focusExam:'ENEM',pismModule:'I',enemDate:'2026-11-08',pismDate:'2026-12-05'},...load(K.settings,{})});
const familyKey=q=>`${q.exam}|${q.module||''}|${q.variantFamily||q.id}`;
let metricsStatsRaw='',metricsStats={},familySummary=new Map(),idMap=new Map();
function refreshBankCache(){const bank=questions();if(idMap.size!==bank.length)idMap=new Map(bank.map(q=>[String(q.id),q]));const raw=localStorage.getItem(K.stats)||'{}';if(raw===metricsStatsRaw)return;metricsStatsRaw=raw;try{metricsStats=JSON.parse(raw)||{}}catch{metricsStats={}}familySummary=new Map();for(const q of bank){const x=metricsStats[q.id];if(!x)continue;const key=familyKey(q),row=familySummary.get(key)||{attempts:0,correct:0,wrong:0};row.attempts+=num(x.attempts);row.correct+=num(x.correct);row.wrong+=num(x.wrong);familySummary.set(key,row)}}
function lastAnswers(){const out={};for(const row of load(K.answers,[])){if(row?.id==null)continue;const id=String(row.id);if(!out[id]||num(row.ts)>num(out[id].ts))out[id]=row}return out}
function activeErrors(){refreshBankCache();const last=lastAnswers(),out=[];for(const q of questions()){if(num(metricsStats[q.id]?.wrong)>0&&(!last[String(q.id)]||last[String(q.id)].ok===false))out.push(String(q.id))}return out}
function cleanReviewQueue(){refreshBankCache();const queue=load(K.review,{});let changed=false;for(const [key,item] of Object.entries(queue)){const id=String(item?.id??key),due=String(item?.due||'');if(!idMap.has(id)||!/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/.test(due)){delete queue[key];changed=true}}if(changed)save(K.review,queue);return queue}
function dueReviews(){const t=today();return Object.values(cleanReviewQueue()).filter(x=>x?.due&&x.due<=t).sort((a,b)=>String(a.due).localeCompare(String(b.due)))}
function upcomingReviews(days=7){const t=today(),d=new Date(t+'T12:00:00');d.setDate(d.getDate()+days);const end=d.toISOString().slice(0,10);return Object.values(cleanReviewQueue()).filter(x=>x?.due&&x.due>t&&x.due<=end).sort((a,b)=>String(a.due).localeCompare(String(b.due)))}
function mastery(exam,module){refreshBankCache();const groups={},seen=new Set();for(const q of questions()){if(q.exam!==exam||(exam==='PISM'&&q.module!==module))continue;const fam=familyKey(q);if(seen.has(fam))continue;seen.add(fam);const key=`${q.subject}|||${q.topic}`,g=groups[key]??={subject:q.subject,topic:q.topic,total:0,answered:0,attempts:0,correct:0,wrong:0},s=familySummary.get(fam)||{attempts:0,correct:0,wrong:0};g.total++;if(s.attempts){g.answered++;g.attempts+=s.attempts;g.correct+=s.correct;g.wrong+=s.wrong}}return Object.values(groups).map(g=>{if(!g.attempts)return{...g,score:0,accuracy:0,coverage:0};const accuracy=g.correct/g.attempts,coverage=g.total?g.answered/g.total:0,experience=Math.min(1,g.attempts/Math.max(2,g.total*2));let score=Math.round((accuracy*.60+coverage*.25+experience*.15)*100);if(g.attempts<2)score=Math.min(score,40);else if(g.total<2)score=Math.min(score,84);return{...g,score,accuracy,coverage}}).sort((a,b)=>(a.attempts?0:1)-(b.attempts?0:1)||a.score-b.score||b.wrong-a.wrong)}
function context(){refreshBankCache();const s=settings(),exam=s.focusExam==='PISM'?'PISM':'ENEM',module=s.pismModule||'I',vals=Object.values(metricsStats),answered=vals.reduce((a,x)=>a+num(x?.attempts),0),correct=vals.reduce((a,x)=>a+num(x?.correct),0),sessions=load(K.sessions,[]),t=today(),todayMin=sessions.filter(x=>x?.dateISO===t).reduce((a,x)=>a+num(x.minutes),0),cut=new Date();cut.setDate(cut.getDate()-6);const cutIso=cut.toISOString().slice(0,10),weekMin=sessions.filter(x=>String(x?.dateISO||'')>=cutIso).reduce((a,x)=>a+num(x.minutes),0),target=exam==='ENEM'?s.enemDate:s.pismDate,days=target?Math.max(0,Math.ceil((new Date(target+'T12:00:00')-new Date(t+'T12:00:00'))/86400000)):null,due=dueReviews(),upcoming=upcomingReviews(),errors=activeErrors(),rows=mastery(exam,module);return{s,exam,module,target,days,answered,correct,accuracy:answered?Math.round(correct/answered*100):0,todayMin,weekMin,dailyGoal:Math.max(20,num(s.dailyGoal,60)),due,upcoming,errors,rows,weak:rows.find(x=>x.attempts)||null}}
function decide(ctx){const remaining=Math.max(0,ctx.dailyGoal-ctx.todayMin),minutes=Math.max(25,Math.min(70,remaining||40)),weak=ctx.weak;let type='diagnostic',title='Crie uma base de diagnóstico.',reason='Faça um bloco curto de questões inéditas para o plano começar a identificar lacunas reais.',primary='Iniciar diagnóstico',subject='',topic='';if(ctx.due.length){type='review';title='Revisões pendentes vêm primeiro.';reason=`Você tem ${ctx.due.length} revisão${ctx.due.length===1?'':'ões'} para hoje ou atrasada${ctx.due.length===1?'':'s'}. Limpar essa fila evita acumular conteúdo esquecido.`;primary='Revisar agora'}else if(ctx.errors.length>=3){type='errors';title='Recupere seus erros ativos.';reason=`Há ${ctx.errors.length} erro${ctx.errors.length===1?'':'s'} ainda sem recuperação confirmada. Feche essas lacunas antes de aumentar o volume.`;primary='Treinar meus erros'}else if(weak){type='weak';subject=weak.subject||'';topic=weak.topic||'';title=`Prioridade: ${topic||subject}.`;reason=`Este é o ponto estudado com menor domínio estimado (${Math.round(num(weak.score))}/100) no seu recorte atual.`;primary='Treinar prioridade'}else if(ctx.answered>=5){type='mixed';title='Consolide o que você já começou.';reason='Você já tem uma amostra inicial. Faça um bloco misto para ampliar cobertura sem abandonar revisão.';primary='Continuar treino'}const a=Math.max(5,Math.min(15,Math.round(minutes*.24))),b=Math.max(12,Math.min(35,Math.round(minutes*.52))),c=Math.max(6,minutes-a-b);const steps=type==='review'?[['Revisões pendentes',Math.max(10,Math.round(minutes*.45)),'recupere o que venceu'],['Questões de confirmação',Math.max(10,Math.round(minutes*.35)),'confirme o aprendizado'],['Fechamento',Math.max(5,minutes-Math.max(10,Math.round(minutes*.45))-Math.max(10,Math.round(minutes*.35))),'registre o que ainda travou']]:[['Revisão objetiva',a,type==='weak'?(subject||'prioridade atual'):'prepare o foco'],['Questões dirigidas',b,type==='errors'?'erros ativos':'prática com correção'],['Recuperação',c,'refaça o que ainda não fixou']];return{type,title,reason,primary,subject,topic,minutes,steps,confidence:ctx.answered>=30?'alta':ctx.answered>=10?'média':'inicial'}}
let homeScheduled=false;
function scheduleHome(){if(homeScheduled)return;homeScheduled=true;setTimeout(()=>{homeScheduled=false;applyHome()},40)}
function applyHome(){const root=document.getElementById('v9AdaptiveHome');if(!root||root.hidden||!root.querySelector('.v9-shell'))return;const ctx=context(),d=decide(ctx),title=root.querySelector('.v9-priority h2'),reason=root.querySelector('.v9-reason'),eyebrow=root.querySelector('.v9-priority-eyebrow'),status=root.querySelector('.v9-status'),plan=root.querySelector('.v9-plan'),button=root.querySelector('.v9-btn-primary');setText(title,d.title);setText(reason,d.reason);setText(eyebrow,`Sessão recomendada · ${d.minutes} min`);setHtml(status,`<i class="v9-status-dot"></i>Confiança ${esc(d.confidence)}`);const planHtml=d.steps.map((x,i)=>`<div class="v9-plan-step"><span>Etapa ${i+1} · ${x[1]} min</span><strong>${esc(x[0])}</strong><small>${esc(x[2])}</small></div>`).join('');setHtml(plan,planHtml);if(button){const html=`<i data-lucide="play" class="icon"></i>${esc(d.primary)}`;if(button.innerHTML!==html)button.innerHTML=html;button.onclick=()=>startPrimary(d)}const reading=root.querySelectorAll('.v9-lower-grid .v9-topic-list');if(reading[0]){const rows=ctx.rows.filter(x=>x.attempts).slice(0,3),html=rows.length?rows.map(x=>`<div class="v9-topic" role="group"><div><b>${esc(x.subject)}</b><small>${esc(x.topic||'Visão geral')} · ${Math.round(x.accuracy*100)}% de acerto</small></div><span class="v9-score">${Math.round(x.score)}/100</span></div>`).join(''):'<div class="v9-empty">Resolva algumas questões para identificar seus pontos de maior retorno.</div>';setHtml(reading[0],html)}if(reading[1]){const html=`<div class="v9-topic"><div><b>Revisões</b><small>conteúdo no momento de recuperação</small></div><span class="v9-score">${ctx.due.length}</span></div><div class="v9-topic"><div><b>Erros ativos</b><small>erros sem recuperação confirmada</small></div><span class="v9-score">${ctx.errors.length}</span></div><div class="v9-topic"><div><b>Amostra</b><small>respostas usadas no diagnóstico</small></div><span class="v9-score">${ctx.answered}</span></div>`;setHtml(reading[1],html)}const rhythm=root.querySelector('.v9-insight strong');setText(rhythm,ctx.due.length?`${ctx.due.length} revisão${ctx.due.length===1?'':'ões'} para hoje`:ctx.upcoming.length?`${ctx.upcoming.length} nos próximos 7 dias`:'Nenhuma revisão urgente');root.dataset.stableContext=VERSION;safe(()=>window.lucide?.createIcons?.())}
function startPrimary(d=decide(context())){const exam=settings().focusExam==='PISM'?'PISM':'ENEM';if(d.type==='review'){window.go?.('reviews');return}if(d.type==='errors'){window.openQuestionMode?.(exam);setTimeout(()=>{const el=document.getElementById('qStatus');if(el){el.value='WRONG';el.dispatchEvent(new Event('change',{bubbles:true}))}},90);return}if(d.type==='weak'&&d.subject){window.openQuestionMode?.(exam);setTimeout(()=>{const sub=document.getElementById('qSubject');if(sub){sub.value=d.subject;sub.dispatchEvent(new Event('change',{bubbles:true}))}const top=document.getElementById('qTopic');if(top&&d.topic){top.value=d.topic;top.dispatchEvent(new Event('change',{bubbles:true}))}},110);return}window.openQuestionMode?.(exam)}
function syncQuestionSeparation(){const s=settings(),exam=document.getElementById('qExam'),module=document.getElementById('qModule');if(exam&&exam.value!=='ENEM'&&exam.value!=='PISM')exam.value=s.focusExam==='PISM'?'PISM':'ENEM';if(module){module.disabled=exam?.value!=='PISM';if(exam?.value!=='PISM')module.value='ALL';else if(module.value==='ALL')module.value=s.pismModule||'I'}document.querySelectorAll('[data-qexam-switch]').forEach(btn=>btn.setAttribute('aria-pressed',String(btn.getAttribute('data-qexam-switch')===(exam?.value||s.focusExam))))}
function wrapReviews(){const dueBase=window.startDueReview,errorBase=window.startErrorDrill,singleBase=window.startSingleReview;if(typeof dueBase==='function'&&!dueBase.__v92){const fn=function(){if(!dueReviews().length&&!activeErrors().length){window.toast?.('Sua fila de revisão está em dia.');return}cleanReviewQueue();return dueBase.apply(this,arguments)};fn.__v92=true;window.startDueReview=fn}if(typeof errorBase==='function'&&!errorBase.__v92){const fn=function(){if(!activeErrors().length){window.toast?.('Nenhum erro ativo para revisar agora.');return}return errorBase.apply(this,arguments)};fn.__v92=true;window.startErrorDrill=fn}if(typeof singleBase==='function'&&!singleBase.__v92){const fn=function(id){refreshBankCache();if(!idMap.has(String(id))){cleanReviewQueue();window.toast?.('Esta revisão não está mais disponível no banco atual.');safe(()=>window.renderReviewPage?.());return}return singleBase.apply(this,arguments)};fn.__v92=true;window.startSingleReview=fn}}
let planTasks=[];
function planAction(index){const task=planTasks[index];if(!task)return;const ctx=context();if(task.kind==='review')return window.startDueReview?.();if(task.kind==='errors')return window.startErrorDrill?.();if(task.kind==='mock'){window.go?.('mocks');setTimeout(()=>window.openMockConfig?.(ctx.exam),60);return}if(task.kind==='essay'){window.go?.('essay');return}if(task.kind==='discursive'){window.go?.('mocks');setTimeout(()=>{const select=document.getElementById('discModule');if(select){select.value=ctx.module;select.dispatchEvent(new Event('change',{bubbles:true}));select.scrollIntoView({block:'center'})}},90);return}window.openQuestionMode?.(ctx.exam);setTimeout(()=>{const s=document.getElementById('qSubject');if(s&&task.row?.subject){s.value=task.row.subject;s.dispatchEvent(new Event('change',{bubbles:true})}const t=document.getElementById('qTopic');if(t&&task.row?.topic){t.value=task.row.topic;t.dispatchEvent(new Event('change',{bubbles:true}))}},100)}
function enhancePlan(){const host=document.getElementById('weeklyPlan');if(!host)return;const ctx=context(),studied=ctx.rows.filter(x=>x.attempts),unstarted=ctx.rows.filter(x=>!x.attempts),priority=studied.length?studied:unstarted,daily=Math.max(30,Math.min(180,ctx.dailyGoal)),main=Math.max(25,Math.min(65,Math.round(daily*.65))),tasks=[];if(ctx.due.length)tasks.push({kind:'review',title:'Revisões pendentes',meta:`${ctx.due.length} item(ns) vencidos ou para hoje`,minutes:Math.min(main,45),label:'Revisar'});else if(ctx.errors.length)tasks.push({kind:'errors',title:'Recuperação de erros',meta:`${ctx.errors.length} erro(s) ainda ativos`,minutes:Math.min(main,40),label:'Treinar'});else if(priority[0])tasks.push({kind:'topic',row:priority[0],title:`${priority[0].subject} · ${priority[0].topic}`,meta:'bloco de maior retorno',minutes:main,label:'Treinar'});for(let i=tasks.length;i<6;i++){const row=priority[i%Math.max(1,priority.length)]||unstarted[i%Math.max(1,unstarted.length)];tasks.push(row?{kind:'topic',row,title:`${row.subject} · ${row.topic}`,meta:row.attempts?'questões dirigidas + correção':'exploração inicial do assunto',minutes:main,label:'Treinar'}:{kind:'topic',title:'Questões mistas',meta:'amplie a amostra para melhorar o plano',minutes:main,label:'Abrir questões'})}tasks.splice(4,0,ctx.exam==='ENEM'?{kind:'essay',title:'Redação ENEM',meta:'produção + revisão pelas cinco competências',minutes:Math.min(60,daily),label:'Escrever'}:{kind:'discursive',title:`Discursivas PISM ${ctx.module}`,meta:'produção escrita + critérios de autocorreção',minutes:Math.min(50,daily),label:'Praticar'});tasks.length=7;tasks[6]={kind:'mock',title:`Simulado ${ctx.exam}${ctx.exam==='PISM'?` · Módulo ${ctx.module}`:''}`,meta:'bloco de prova para fechar a semana',minutes:Math.min(90,Math.max(30,daily)),label:'Configurar'};planTasks=tasks;const names=['Hoje','Amanhã','Dia 3','Dia 4','Dia 5','Dia 6','Dia 7'],html=tasks.map((task,i)=>`<div class="plan-item v92-plan-item"><div class="plan-num">${i+1}</div><div class="plan-main"><b>${names[i]} · ${esc(task.title)}</b><span>${esc(task.meta)} · ${task.minutes} min</span></div><button class="btn btn-sm ${i===0?'btn-primary':'btn-secondary'}" onclick="GabaritoV92.planAction(${i})">${esc(task.label)}</button></div>`).join('');setHtml(host,html);setText(document.getElementById('planPriority'),tasks[0]?.title||'—')}
function wrapPlan(){const base=window.generatePlan;if(typeof base!=='function'||base.__v92)return;const fn=function(){let out;try{out=base.apply(this,arguments)}catch(e){console.error('[Gabarito+ V9.2] Plano base:',e)}setTimeout(enhancePlan,0);return out};fn.__v92=true;window.generatePlan=fn}
function validMock(m){refreshBankCache();if(!m||!Array.isArray(m.ids)||!m.ids.length)return null;m.ids=m.ids.map(String).filter(id=>idMap.has(id));if(!m.ids.length)return null;m.index=Math.max(0,Math.min(m.ids.length-1,num(m.index)));m.started=num(m.started,Date.now());if(Date.now()-m.started>6*3600000)return null;return m}
function persistMock(){const current=safe(()=>app.mock,null);if(!current){try{sessionStorage.removeItem(K.mockResume)}catch{}return}const copy=safe(()=>JSON.parse(JSON.stringify(current)),null);if(copy)try{sessionStorage.setItem(K.mockResume,JSON.stringify({...copy,savedAt:Date.now()}))}catch{}}
function readMock(){try{return validMock(JSON.parse(sessionStorage.getItem(K.mockResume)||'null'))}catch{return null}}
function clearMock(){try{sessionStorage.removeItem(K.mockResume)}catch{}}
function renderMockResume(){const page=document.getElementById('page-mocks');if(!page)return;document.getElementById('v92MockResume')?.remove();if(safe(()=>app.mock,null))return;const saved=readMock();if(!saved)return;const host=document.createElement('div');host.id='v92MockResume';host.className='card card-pad v92-resume-card';host.innerHTML=`<div><span class="eyebrow">Sessão interrompida</span><strong>Você tem um simulado em andamento.</strong><p>${esc(saved.cfg?.exam||'Prova')} · questão ${saved.index+1} de ${saved.ids.length}. Retome exatamente de onde parou.</p></div><div class="v92-resume-actions"><button class="btn btn-secondary" onclick="GabaritoV92.discardMock()">Descartar</button><button class="btn btn-primary" onclick="GabaritoV92.resumeMock()">Continuar simulado</button></div>`;page.querySelector('.mock-cards')?.insertAdjacentElement('beforebegin',host)}
function resumeMock(){const saved=readMock();if(!saved){window.toast?.('Não há simulado para retomar.');return}app.mock=saved;const active=document.getElementById('mockActive');if(active)active.style.display='block';window.go?.('questions');setTimeout(()=>window.renderMockQuestion?.(),40)}
function discardMock(){clearMock();document.getElementById('v92MockResume')?.remove();window.toast?.('Simulado interrompido descartado.')}
function wrapMock(){const names=['startMock','startCustomMock','startMockPreset','startPremiumPreset','startPismDayMock','answerQuestion','nextMockQuestion','premiumJump','premiumNext','premiumPrev','togglePremiumMark'];for(const name of names){const base=window[name];if(typeof base!=='function'||base.__v92persist)continue;const fn=function(){const out=base.apply(this,arguments);setTimeout(persistMock,0);return out};fn.__v92persist=true;window[name]=fn}const finish=window.finishMock;if(typeof finish==='function'&&!finish.__v92persist){const fn=function(){try{return finish.apply(this,arguments)}finally{clearMock();setTimeout(renderMockResume,0)}};fn.__v92persist=true;window.finishMock=fn}window.addEventListener('beforeunload',persistMock)}
let essayTimer=null,essayItems=[];
function essayState(){const text=document.getElementById('essayText'),title=document.getElementById('essayTitle');if(!text||!title)return null;return{text:text.value,title:title.value,theme:document.getElementById('essayTheme')?.textContent||'',scores:[1,2,3,4,5].map(i=>num(document.getElementById('essayC'+i)?.value)),savedAt:Date.now()}}
function persistEssay(){const state=essayState();if(!state)return;save(K.essayState,state);try{localStorage.setItem(K.draft,state.text)}catch{}setText(document.getElementById('v92EssaySaveState'),'Rascunho salvo')}
function scheduleEssay(){clearTimeout(essayTimer);setText(document.getElementById('v92EssaySaveState'),'Salvando…');essayTimer=setTimeout(persistEssay,350)}
function restoreEssay(){const state=load(K.essayState,null);if(!state||Date.now()-num(state.savedAt)>30*86400000)return;const text=document.getElementById('essayText'),title=document.getElementById('essayTitle');if(text&&!text.value&&state.text)text.value=state.text;if(title&&!title.value&&state.title)title.value=state.title;if(state.theme)setText(document.getElementById('essayTheme'),state.theme);if(Array.isArray(state.scores))state.scores.slice(0,5).forEach((v,i)=>{const el=document.getElementById('essayC'+(i+1));if(el)el.value=Math.max(0,Math.min(200,num(v)))});safe(()=>window.essayMetrics?.());safe(()=>window.updateEssayScoreTotal?.())}
function decorateEssay(){const page=document.getElementById('page-essay');if(!page)return;page.classList.add('v92-manual-essay');const ai=page.querySelector('.ai-correction-card');if(ai)ai.hidden=true;let status=document.getElementById('v92EssaySaveState');if(!status){status=document.createElement('span');status.id='v92EssaySaveState';status.className='v92-save-state';status.textContent='Rascunho salvo';document.getElementById('essayChars')?.parentElement?.append(' · ',status)}for(const id of ['essayText','essayTitle','essayC1','essayC2','essayC3','essayC4','essayC5']){const el=document.getElementById(id);if(el&&!el.dataset.v92Autosave){el.dataset.v92Autosave='1';el.addEventListener('input',scheduleEssay)}}restoreEssay()}
function loadEssay(index){const item=essayItems[index];if(!item)return;const current=document.getElementById('essayText')?.value.trim();if(current&&current!==String(item.text||'').trim()&&!window.confirm('Abrir esta redação e substituir o rascunho atual?'))return;const text=document.getElementById('essayText'),title=document.getElementById('essayTitle');if(text)text.value=item.text||'';if(title)title.value=item.title||'';setText(document.getElementById('essayTheme'),item.theme||document.getElementById('essayTheme')?.textContent||'');if(Array.isArray(item.scores))item.scores.slice(0,5).forEach((v,i)=>{const el=document.getElementById('essayC'+(i+1));if(el)el.value=v});safe(()=>window.essayMetrics?.());safe(()=>window.updateEssayScoreTotal?.());persistEssay();window.go?.('essay')}
function enhanceEssayHistory(){const el=document.getElementById('essayHistory');if(!el)return;essayItems=load(K.essays,[]).slice(0,8);const html=essayItems.length?essayItems.map((x,i)=>`<div class="plan-item v92-essay-history"><div class="plan-main"><b>${esc(x.title||'Redação sem título')}</b><span>${num(x.words)} palavras · ${esc(x.date||'')}${x.score!=null?' · '+num(x.score)+'/1000':''}</span></div><button class="btn btn-sm btn-secondary" onclick="GabaritoV92.loadEssay(${i})">Abrir</button></div>`).join(''):'<div class="empty">Nenhuma redação salva.</div>';setHtml(el,html)}
function wrapEssay(){const saveBase=window.saveEssay;if(typeof saveBase==='function'&&!saveBase.__v92){const fn=function(){const before=load(K.essays,[]).length,out=saveBase.apply(this,arguments),after=load(K.essays,[]).length;if(after>before){localStorage.removeItem(K.essayState);setTimeout(enhanceEssayHistory,0)}return out};fn.__v92=true;window.saveEssay=fn}const renderBase=window.renderEssayHistory;if(typeof renderBase==='function'&&!renderBase.__v92){const fn=function(){safe(()=>renderBase.apply(this,arguments));enhanceEssayHistory()};fn.__v92=true;window.renderEssayHistory=fn}const themeBase=window.newEssayTheme;if(typeof themeBase==='function'&&!themeBase.__v92){const fn=function(){const out=themeBase.apply(this,arguments);scheduleEssay();return out};fn.__v92=true;window.newEssayTheme=fn}}
function pageRefresh(page){if(page==='home')scheduleHome();if(page==='questions')setTimeout(()=>{syncQuestionSeparation();safe(()=>window.renderQStats?.())},35);if(page==='reviews')setTimeout(()=>{cleanReviewQueue();safe(()=>window.renderReviewPage?.())},35);if(page==='plan')setTimeout(enhancePlan,35);if(page==='mocks')setTimeout(renderMockResume,35);if(page==='essay')setTimeout(()=>{decorateEssay();enhanceEssayHistory()},35);safe(()=>window.lucide?.createIcons?.())}
function wrapGo(){const base=window.go;if(typeof base!=='function'||base.__v92stable)return;const fn=function(page){try{const out=base.apply(this,arguments);pageRefresh(page);return out}catch(e){console.error('[Gabarito+ V9.2] Falha ao abrir página',page,e);document.querySelectorAll('.page').forEach(x=>x.classList.toggle('active',x.id==='page-'+page));pageRefresh(page);window.toast?.('A página foi recuperada após um erro de carregamento.')}};fn.__v92stable=true;window.go=fn}
function health(){const required=['page-home','page-questions','page-reviews','page-plan','page-mocks','page-essay','questionCard','reviewQueueList','weeklyPlan','mockConfig','essayText'],missing=required.filter(id=>!document.getElementById(id));return{version:VERSION,ok:missing.length===0,missing,questions:questions().length,reviewQueue:Object.keys(cleanReviewQueue()).length}}
function boot(){wrapReviews();wrapPlan();wrapMock();wrapEssay();wrapGo();cleanReviewQueue();decorateEssay();enhanceEssayHistory();renderMockResume();scheduleHome();const home=document.getElementById('page-home');if(home)new MutationObserver(scheduleHome).observe(home,{childList:true,subtree:true});document.addEventListener('visibilitychange',()=>{if(!document.hidden){scheduleHome();renderMockResume()}});window.addEventListener('storage',()=>{metricsStatsRaw='';scheduleHome()});const h=health();if(!h.ok)console.warn('[Gabarito+ V9.2] Elementos ausentes:',h.missing)}
window.GabaritoV92={version:VERSION,health,refresh:()=>pageRefresh(safe(()=>app.page,'home')),context,decide,startPrimary,planAction,resumeMock,discardMock,loadEssay};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,260),{once:true});else setTimeout(boot,260);
})();
