import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
test('seed preserva banco editorial',()=>{const q=JSON.parse(fs.readFileSync(path.join(root,'supabase/seeds/questions.json'),'utf8')),d=JSON.parse(fs.readFileSync(path.join(root,'supabase/seeds/pism-discursives.json'),'utf8'));const manifest=JSON.parse(fs.readFileSync(path.join(root,'data/question-bank-manifest.json'),'utf8'));assert.equal(q.length,manifest.published);assert.equal(q.filter(x=>x.exam==='ENEM').length,manifest.byExam.ENEM);assert.equal(q.filter(x=>x.exam==='PISM').length,manifest.byExam.PISM);assert.equal(d.length,52);assert.equal(new Set(q.map(x=>x.id)).size,q.length);for(const x of q)assert.ok(Number.isInteger(Number(x.answer))&&Number(x.answer)>=0&&Number(x.answer)<=4)});
