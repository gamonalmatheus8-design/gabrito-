import test from 'node:test';import assert from 'node:assert/strict';import {spawn} from 'node:child_process';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
test('servidor Gabarito+ responde health e home',async()=>{const port=3190,p=spawn(process.execPath,['server.mjs'],{cwd:root,env:{...process.env,PORT:String(port)},stdio:'ignore'});try{await new Promise(r=>setTimeout(r,500));const h=await fetch(`http://127.0.0.1:${port}/api/health`).then(r=>r.json());assert.equal(h.version,'gabarito-mais-1.1.0');assert.equal((await fetch(`http://127.0.0.1:${port}/`)).status,200);assert.equal((await fetch(`http://127.0.0.1:${port}/supabase/schema.sql`)).status,403)}finally{p.kill()}});
